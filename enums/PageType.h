#ifndef PAGETYPE_H
#define PAGETYPE_H

namespace enums
{
    // page type enum for the wordle game to identify different game pages
    //
    enum PageType {LOGIN_PAGE = 0, SETTINGS_PAGE = 1, GAME_PAGE = 2, GAME_OVER_PAGE = 3};
}

#endif // PAGETYPE_H
